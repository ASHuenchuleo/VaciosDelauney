/* Prototipos de funciones para examinar grafos de adyacencia. */

extern void adj_graph_DFS(int i, int j, int *adj, int *visited, double *area, int *root_id,
										int *is_seed, double *total_area, int *has_seed,
										int *touches_border, int *total_triangles);
